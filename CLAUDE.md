# SK하이닉스 다이싱 공정 품질 관리 — 14조

새 세션이 이 파일을 자동으로 읽습니다. **긴 설명 대신 "어디를 보면 되는지"만 적습니다.**

## 작업 방식

- 작업 브랜치는 `김시우`. main에는 GitHub PR로만 올립니다(PR #14~#22가 전부 이 패턴).
- 커밋 메시지는 **무엇을 고쳤나보다 왜 그렇게 판단했나**를 적습니다. 기존 커밋 로그를
  보면 형식이 나옵니다 — 반증 데이터와 기각한 대안을 같이 남깁니다.
- 숫자를 말할 때는 **직접 재서** 말합니다. 문서에 적힌 값을 그대로 옮기지 않습니다.

## 절대 어길 수 없는 규칙

**1. 인자↔불량 짝짓기의 단일 출처는 관계DB입니다.**
`26.08.05_Goal2_통합_Relationship_DB_JHdaimma/agent_cause_factors.json` (윤진혁님 작업).
우리 코드에 짝을 하드코딩하지 않습니다. DB가 바뀌면 자동으로 따라가야 합니다.

**2. tier가 취급법을 정합니다.** DB의 `tier_legend`가 정의해뒀고 그게 유일한 판단 근거입니다.
```
T1/T2  원인 — 이름 부름 + (alert_usable이면) 경계값 사용
M1     감시지표 — 이름 부름, 조치 지시 금지
T3     후보 — "원인이라고 답하지 말 것"
T4     판단보류 — 도메인과 데이터 방향이 반대
```
`alert_usable`은 tier와 **별개 축**입니다. "위험구간 선을 그을 수 있는가"만 말합니다.
False는 "아직 안 봤다"가 아니라 **"재봤는데 안 갈렸다"**는 뜻입니다.

**3. Health Index는 변수에서만 계산됩니다.**
`margin = (현재값 − 정상값) / (관리한계 − 정상값)`, 관리한계 = 정상값 ± 3σ(Shewhart).
defect 점수와 장비 점수는 계산이 아니라 **그 아래 최악 변수의 점수를 그대로 복사한 것**입니다.
불량이 실제로 몇 건 났는지는 점수에 **안 들어갑니다**(occurrence로 따로 나갑니다).

**4. 경보는 변수에 붙습니다.** defect는 경보를 받지 않습니다.
`trend_analysis.py`가 (장비, 컬럼, Product, Recipe) 시계열에 CUSUM(K=0.7σ, H=4.5σ)을 돌립니다.
`DEFECT_TO_FACTORS`는 경보 목록이 **아니라** "이 불량의 확정 원인이 뭐냐" 역인덱스입니다.
**경보가 켜졌다(`early_warning_active`)와 큰 경보다는 다릅니다.** 세기는 `alert_level` —
`"full"`(점수를 최대폭까지 깎은 경보, 10건)만 강조 표시를 받고 `"early"`(65건, HI 전부
78.0 이상)는 흐리게 나갑니다. boolean만 보고 빨갛게 칠하지 마세요.

**5. "스펙아웃"과 "관리한계 초과"는 다릅니다.**
89일간 멘토 스펙 위반 지속 경보는 0건이고 `spec_status`는 전 장비 OK입니다.
HI 10점 미만 = 관리한계(3σ) 초과이지 스펙아웃이 아닙니다.

**6. 도메인 방향과 반대로 학습된 그룹은 그 짝의 근거로 쓰지 않습니다.**
C유형 경계값은 Product×Recipe 54개 그룹마다 따로 학습하는데, 그때 위험 방향도 같이
나옵니다. 그게 관계DB(`rel_30.threshold_direction`)와 반대인 그룹은 `00_baseline_C.csv`에
행을 안 만듭니다(`compute_baseline_type_c`). 방향의 단일 출처도 DB이고 우리가 데이터에서
다시 정하지 않습니다 — 규칙 1이 방향에서만 깨지면 안 됩니다. DB에 없는 짝(경보전용 등)은
비교 대상이 없어 그대로 둡니다. 제외되면 실행 로그에 `[방향불일치]`로 찍힙니다.

실제 사례가 `CLN_Flow↔Particle`입니다 — 54그룹 중 7개가 "높으면 위험"으로 나왔는데
**세정 유량이 높아서 파티클이 는다는 건 물리적으로 말이 안 됩니다.** 그 7개에서 잡힌 건
이 인자가 아니라 다른 원인입니다. 제외하니 DP03 CLN_Flow의 10일짜리 경보가 통째로
사라졌고(오탐), 경보행이 9,941 → 9,701로 줄었습니다. DP01의 5.2일 경보는 정상 방향
그룹에서 나온 것이라 그대로 남습니다.

## 지금 열려 있는 사안

| 사안 | 상태 | 문서 |
|---|---|---|
| ~~라벨 확장안(`pure` → 이진)~~ | **26.08.11 적용**(PR #24, 윤진혁) | `.../회의자료_라벨확장안_결정요청.md` |
| ~~DP04 Particle 95.0~~ | **해소 — 14.5** (위 적용 결과) | 같은 문서 |
| Micro_Crack 감시 | 원인은 말할 수 있게 됨(`Cooling_Flow`). 장비 점수엔 여전히 불참(`repro_state=실패`) | `.../발견_라벨정의가_MicroCrack을_가렸다.md` |
| ~~`CLN_Flow↔Particle` 방향 불일치~~ | **26.08.11 해소** — 도메인 방향과 반대인 그룹은 제외 | 아래 설명 |
| 에이전트 답변 실제 검증 | 배선 완료(`.env` + `--check`), **유효한 키로는 미검증** | `26.08.01_Goal_AI_Agent_Prototype_김시우/README.md` |

**한 인자에 defect가 둘인 경우가 이제 실제로 있습니다** — `CLN_Flow`와 `CLN_Pressure`가
각각 Remain_Coat(T1)와 Particle(T2)의 원인입니다. 파이프라인은 **짝(인자, defect)을
단위로** 다룹니다. 경계값·평소 진입률·경보가 짝마다 따로 나옵니다(실측: `CLN_Flow`는
경계값 중앙값이 Remain_Coat 기준 9.7027 / Particle 기준 9.7355이고, 두 짝이 다 학습된
47그룹 중 40개에서 다름 — 방향 필터로 빠진 7그룹은 비교 대상이 아님).
컬럼당 하나를 골라야 하는 자리(`build_health_index`의 화면 대표값)에서는 **파일 행 순서가
아니라 가장 급한 tier**를 씁니다. 재현 검증은 `docs/check_multi_defect_pairing.py`.

방향이 도메인과 반대로 학습된 그룹은 제외됩니다 — 규칙 6 참고. `CLN_Flow↔Particle`이
그 사례라 54그룹이 아니라 47그룹입니다.

## 검증 방법

```bash
python3 "26.08.01_Goal5_HealthIndex_Dashboard_김시우/build_health_index.py"
```
장비 점수가 **DP01 85.0 / DP02 47.6 / DP03 50.8 / DP04 14.5**로 나오면 정상입니다.
재생성 후 `git status`에 `generated_at` 한 줄만 바뀌어야 합니다.
(DP01은 라벨 확장안 적용 전 91.9였습니다 — `CLN_Flow`가 Particle 원인으로도 잡히면서
그 경보가 변수 점수를 깎았습니다. 점수가 내려간 게 아니라 **안 보이던 게 보인 것**입니다.)

대시보드(그래프·히트맵은 API 키 없이 동작):
```bash
python3 "26.08.01_Goal_AI_Agent_Prototype_김시우/server.py"
```
챗봇 키는 저장소 루트 `.env`의 `ANTHROPIC_API_KEY`를 읽습니다(`export`도 그대로 동작하고
export가 우선). 연결만 확인하려면 — 토큰을 쓰지 않습니다:
```bash
python3 "26.08.01_Goal_AI_Agent_Prototype_김시우/agent.py" --check
```

## 토큰 절약

이 저장소는 파일이 큽니다(`agent.py` 59KB, `dashboard.html` 43KB, 원자료 10만 행).
전체를 읽지 말고 `grep`으로 필요한 구간만 보세요. 주제가 바뀌면 새 세션을 여세요.

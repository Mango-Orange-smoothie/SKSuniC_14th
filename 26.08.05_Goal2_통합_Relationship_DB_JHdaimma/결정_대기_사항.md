# 결정 대기 사항

작성 JHdaimma · 2026-08-06 기준
관련 산출물 `rel_20_tier_table.csv` · `rel_01_factors.csv` · `TIER_기준.md`

> 이 문서는 **아직 결론이 안 난 것만** 모았습니다.
> 각 항목에 **누가 정해야 하는지**와 **안 정하면 무엇이 잘못되는지**를 적었습니다.

---

## 📌 한눈에

| 구분 | 건수 | 누가 |
|---|---|---|
| **🔴 A-즉시. 지금 처리** | **3** | **JHdaimma 작업 + 김시우님 요청** |
| A. 판정 기준 — 제가 막혀 있는 것 | 2 (+**해소 1**) | JHdaimma 단독 결정 불가 → 팀 |
| B. 팀원 간 결론 충돌 | 3 | 팀 회의 |
| C. 멘토 확인 필요 | 6 | 멘토 |
| D. 기록만 (결정 아님) | 5 | — |

---

# 🔴 A-즉시. 지금 바로 처리해야 하는 것 3건 (2026-08-08 추가)

> 아래 3건은 **대화 중에만 있고 아직 산출물에 반영되지 않은 것**들입니다.
> 잊어버리면 다시 찾기 어려우니 여기 먼저 적습니다.

---

## 즉시-1 🔴 `pipeline/common.py`가 `repro_state == "통과"`만 채택합니다

### 무엇을 발견했나

김시우님 전처리 파이프라인이 제 DB를 읽는데, **채택 조건이 `repro_state == "통과"`** 입니다.

```python
# origin/김시우  pipeline/common.py:138-146
채택 조건은 두 파일의 AND다 — rel_30(인계 파일)의 alert_usable이 True이고,
rel_20(근거표)의 repro_state가 "통과"인 짝만 쓴다.

iface = config.RELATIONSHIP_DB_DIR / "rel_30_trend_interface.csv"
tiers = config.RELATIONSHIP_DB_DIR / "rel_20_tier_table.csv"
```

### 그래서 무슨 일이 생기나

**Chipping T1 3개 + T2 1개가 전처리 단계에서 전부 빠집니다.**

| tier | defect | 인자 | repro_state | 결과 |
|---|---|---|---|---|
| T1 | Chipping | `Power_Efficiency` | **판정불가(원본 표본부족)** | ❌ 탈락 |
| T1 | Chipping | `Laser_Power` | **판정불가** | ❌ 탈락 |
| T1 | Chipping | `Head_Temp` | **판정불가** | ❌ 탈락 |
| T2 | Chipping | `Cooling_Flow` | **판정불가** | ❌ 탈락 |

### 왜 문제인가

제가 `repro_state`를 **3값으로 나눈 이유**가 이겁니다.

| 값 | 뜻 | 처리해야 할 방식 |
|---|---|---|
| `통과` | 재봤고 일치 | 채택 |
| **`판정불가(원본 표본부족)`** | **표본이 없어 못 쟀음** | **채택** (실패 아님) |
| `실패(데이터셋간 방향 불일치)` | 재봤는데 어긋남 | 제외 |

**"못 쟀다"를 "틀렸다"로 처리하면 강한 인자가 부당하게 강등됩니다.**
Chipping은 원본에 pure 표본이 **3건**뿐이라 계산 자체가 불가능한 것이지,
결과가 어긋난 게 아닙니다.

### 요청할 것

```python
# 지금
adopt = alert_usable and repro_state == "통과"

# 이렇게
adopt = alert_usable and repro_state != "실패(데이터셋간 방향 불일치)"
```

### ⚠️ 주의 — 제가 한 번 잘못 판단했던 건입니다

`build_health_index.py`의 `_usable_defects()`만 보고 *"코드는 `repro_state`를 안 쓴다"*고
판단해 회신 ④를 취소했었습니다. **`pipeline/` 폴더를 안 봐서 생긴 오판**입니다.
`build_health_index.py`는 안 쓰지만 **`pipeline/common.py`는 씁니다.**

> 다만 **장비 대표 점수에서 Chipping을 뺀 것(`b85650f`)은 별개이고 타당합니다.**
> 그건 "검증 안 된 인과로 순위 매기지 않는다"는 원칙이고, Chipping HI는 계속 산출돼
> "예방 정비 대상"으로 나갑니다. **여기서 말하는 건 전처리 채택 조건입니다.**

---

## 즉시-2 🟡 `rel_30_trend_interface.csv`에 CUSUM 필드가 없습니다

`rel_28`은 고쳤는데 **`rel_30`은 그대로**입니다. 같은 폴더에 두 방식이 섞여 있습니다.

### 삭제할 열 2개 (제가 잘못 가정한 방식)

```
current_slope_per_day    "하루에 얼마나 오르나"   → 안 씀
trend_window_days        "며칠치를 보나"          → 안 씀
```

### 신설할 열 4개 (실제 구현)

| 열 | 값 |
|---|---|
| `cusum_K` | 0.7σ |
| `cusum_H` | 4.5σ |
| `trend_test` | Mann-Kendall |
| `trend_alpha` | 0.05 |

### 유지할 열 2개 (김시우님이 채우실 칸)

`days_to_threshold` · `trend_status`

### 근거

김시우님 커밋 `1947b31` — **"CUSUM 경로를 전 컬럼으로 확대"**

> *"35개 컬럼 × 4대 전수 측정 결과 오경보율 0.000~0.364%로 전부 통과"*

**Vibration만이 아니라 35개 컬럼 전부에 CUSUM이 적용됩니다.**

---

## 즉시-3 🔴 `Coating_Thickness ↔ Remain_Coat` 판정 회신 — 김시우님 대기 중

원문: `origin/김시우:JHdaimma_요청4_정정.md` (커밋 `f38560b`)

### 김시우님 근거 (원본 데이터 기준)

| 인자 | 분리배수 | 방향일관성 | 학습 그룹 | 상태 |
|---|---|---|---|---|
| `CLN_Flow` | 20.93 | 1.000 | 52/54 | T1 채택됨 |
| `CLN_Pressure` | 19.81 | 1.000 | 54/54 | T1 채택됨 |
| **`Coating_Thickness`** | **18.52** | **1.000** | **54/54** | **미평가** |
| (4위) `Process_Time` | 2.86 | 0.500 | 40/54 | 천장(3.38) 아래 |

**4위와 6.5배 격차입니다.**

### 제 방법으로 재본 결과 (원본, pure 라벨)

| 인자 | 위험구간 발생률 | lift |
|---|---|---|
| **`Coating_Thickness`** (<9.699) | **13.30%** | **18.76** |
| `CLN_Pressure` (T1) | 6.72% | 14.82 |
| `CLN_Flow` (T1) | 9.37% | 10.84 |

**기존 T1 두 개보다 강합니다.** 원본 Cliff's delta −0.3121.

### 스펙으로는 못 잡습니다

```
학습 경계 9.699  vs  멘토 LSL 9.5
위험구간의 98%가 스펙 안쪽 (스펙 위반은 10만 샷 중 45건)
```

커버리지 **69.3% → 94.8%**

### 🔴 그런데 제 체계로는 판정할 수 없습니다

**이유 1 — 확정 도메인 11건에 없습니다**

```
Remain_Coat 도메인:  CLN_Pressure 감소 → 증가
                     CLN_Flow 감소 → 증가
                     (Coating_Thickness 없음)
```

김시우님 이해가 정확합니다 — *"기각된 게 아니라 평가 대상이 아니었던 것"*

**이유 2 — 전성재님이 보류를 걸어두셨습니다**

`REM_COAT_유효인자_정리.md` 16절:

> **`Coating_Thickness` | 분류 보류**
> *"**측정 시점(가공 전/후) 미확인** — 원인인지 불량결과와 동어반복인지조차 아직 불명확.
> **현업 확인 전까지 어느 분류에도 넣지 말 것**"*

```
가공 전 측정?  →  "코팅이 얇으면 잔류가 는다"      ← 물리적으로 이상
가공 후 측정?  →  Remain_Coat와 같은 걸 두 번 잼   ← 동어반복
```

**측정 시점을 모르면 18.76배가 인과인지 동어반복인지 구분이 안 됩니다.**

### 회신할 내용

**🔵 "현장 확인 요청"** — 확정도 기각도 아님.
**멘토님께 물어볼 것: *"`Coating_Thickness`는 가공 전 측정입니까, 후 측정입니까?"***
**이 답 하나로 판정이 갈립니다.**

---

## 참고 — 회신 문서도 같이 고쳐야 합니다

`김시우님_협의요청_회신.md`의 **④**를 다시 써야 합니다.
지금은 *"확인 부탁드립니다"* 톤인데, **즉시-1이 실제 문제**로 확인됐으므로
*"`pipeline/common.py:139`를 고쳐주세요"* 로 격상해야 합니다.

---
---

# A. 판정 기준 — 제가 막혀 있는 것

## A-0. ~~`CLN_Pressure` 급락 기준~~ — **해소됨 (2026-08-08)**

*"급락 기준을 누가 정할 것인가"*가 미결이었는데, **급락 사건 자체가 없었습니다.**

낙차(직전 20샷 이동중앙값 대비)를 실제로 재본 결과:

| | |
|---|---|
| 낙차 분포 | p1 −4.82 ~ p99 +4.83 — **매 샷 ±5로 흔들림** |
| 낙차 > 2.0 | **전체의 16.8%** — 급락이라 부를 수 없음 |
| **절대값 < 297.272** | 알람 8.6% · **lift 3.74** ✅ |
| 낙차 > 2.0 | 알람 16.8% · lift 2.33 |

**절대값 경계가 알람 절반으로 정확도 1.6배 높습니다.**

→ `watch_mode`를 **`spike` → `level`**로 정정. **별도 급락 규칙이 필요 없습니다.**
근거: `rel_31_cln_pressure_spike_check.csv`

---

## A-1. `Particle / CLN_Flow` 를 T2에서 T3로 내릴 것인가

**현재 상태**: T2 (조건부조치)

| 검정 | 결과 |
|---|---|
| 도메인 | ✅ 확정 (CLN_Flow 하락 → Particle 증가) |
| 방향 | ✅ 일치 (delta 음수) |
| 통계검정 | ❌ **delta −0.005** — 사실상 0 |
| RandomForest | ✅ 상위 10위 이내 |
| 경보 방향 | ❌ **역전** — 위험구간 4.24% < 정상구간 5.30% (0.8배) |

**문제**: T2는 "조건부조치" 등급인데 **경보를 걸 수 없습니다.**
Agent가 *"조치하십시오"*라고 말할 근거가 RandomForest 순위 하나뿐이고,
그마저 단변량 신호가 0이며 경보 방향까지 반대입니다.

**제 의견**: **T3(감시)로 내리는 게 맞습니다.**

**안 정하면**: Agent가 근거 없이 세정 유량 조치를 지시하게 됩니다.

---

## A-2. 티어표가 두 개입니다 — 어느 것이 정본인가

| 파일 | 기준 | 건수 |
|---|---|---|
| `rel_12_tiers.csv` | **도메인 게이트 적용 전.** 담당자 판정 + Jun 교차대조로 매김 | 44행 |
| **`rel_20_tier_table.csv`** | **확정 도메인 13건만** 후보로 두고 통계검정 + RF로 매김 | 13행 |

**두 파일의 티어 이름 체계도 다릅니다.**

```
rel_12 :  C1 C2 C3 / M1 M2 M3 / P1 P2
rel_20 :  T1 T2 T3 T4 / M1
```

**제 의견**: **`rel_20`이 정본**입니다. 멘토 방향성(확정 도메인만, FDC-FDC 미연결)을 반영한 최신본입니다.
`rel_12`는 **삭제하지 않고** 이력으로 남기되, 사용하지 않습니다.

**안 정하면**: 김시우님이 Agent에 어느 표를 물릴지 헷갈립니다.

---

# B. 팀원 간 결론 충돌

## B-1. `Particle / Vibration` — daeho님 확정 vs 새 도메인 목록에 없음

| 출처 | 판정 |
|---|---|
| daeho (26.08.05) | **confirmed** — 유일한 원인 후보. 선행신호 잔존율 33.5% |
| Jun 통합본 | Tier2d (관찰만) — not_reproduced |
| **새 확정 도메인 13건** | **목록에 없음** |

**추가 문제**: daeho님 판정의 근거가 **넓은 라벨(broad)에 의존**합니다.

```
broad   +0.317
primary +0.087   ← 팀 기준 0.2 미달
```

**제가 Micro_Crack에서 저지른 실수와 같은 패턴**입니다.

**충돌의 원인**: 비교군 정의 차이. daeho님 r1 비교군의 **38.1%가 불량**이라
r1 Vibration이 비교군에 따라 **−0.024 ~ +0.289**로 갈립니다.

**안 정하면**: Particle에 확정 원인이 있는지 없는지가 안 정해집니다.

---

## B-2. `Chipping / Laser_Centering_Position` — 새 도메인 목록에서 빠짐

| 출처 | 판정 |
|---|---|
| JHdaimma 기존 | confirmed_cause (delta +0.607) |
| Jun 통합본 | Tier2d (not_reproduced) |
| **새 확정 도메인 13건** | **목록에 없음** |

**확인 필요한 이유**: 멘토 인과사슬의 **마지막 칸**입니다.

```
Head_Temp → 크리스탈 스팟온도 → 굴절률 → [ 센터링 ] → Chipping
```

`Head_Temp`는 목록에 넣으셨는데 센터링만 빠졌습니다. **의도인지 누락인지** 확인이 필요합니다.

> 단, 멘토님이 **"FDC-FDC 엮지 말 것"**이라 하셨으므로 사슬 자체는 쓰지 않습니다.
> `Laser_Centering_Position → Chipping` **1:1로만** 넣을지의 문제입니다.

---

## B-3. 감시지표 8개를 어떻게 할 것인가

새 확정 도메인 목록에 **감시지표가 `Surface_Roughness`(Particle) 하나뿐**입니다.
기존에 확정이던 감시지표들이 전부 빠졌습니다.

| 인자 | 기존 판정 | 비고 |
|---|---|---|
| `Kerf_Width_Profile` | confirmed_monitor | delta +0.931 |
| `Bottom_Kerf` `Top_Kerf` | confirmed_monitor | delta +0.93 |
| **`Groove_Depth`** | confirmed_monitor | **팀 설계서 C유형에 명시돼 있음** |
| `Laser_Cleaning_Demand` | confirmed_monitor | 파생 피처 |
| `Package_Size_Asymmetry` | confirmed_monitor | 멘토 힌트 → 김시우님 수식화 |
| `Surface_Roughness`(Chipping/Micro_Crack) | confirmed_monitor | 결과 공변 |

**제 의견**: 주신 목록이 **"원인"만**이라면 정상입니다. 감시지표는 **별도 트랙**으로 유지하면 됩니다.
다만 `Groove_Depth`는 **팀 설계서에 명시**돼 있어서 빼는 게 맞는지 확인이 필요합니다.

**안 정하면**: Agent가 Chipping 경보를 거의 못 냅니다 (경보용 감시지표가 사라짐).

---

# C. 멘토 확인 필요

## C-1. SOP — **미수령**

`rel_20_tier_table.csv`의 `sop_action` `sop_check` 칸이 **비어 있습니다.**
`sop_status = "SOP 미수령 — 멘토 제공 대기"`

**`action_type`(즉시조치/조건부조치/감시/추세알람/급락알람)은 이미 채워져 있으므로**,
SOP 문구만 받으면 그 자리에 넣으면 됩니다.

---

## C-2. 조치 난이도표

각 원인 인자를 **실제로 얼마나 빨리 바꿀 수 있는지** 모릅니다.

```
Laser_Power   → 다이얼 조정?  즉시 가능?
Cooling_Flow  → 필터 교체?    라인 정지 필요?
Vibration     → 정비 일정?    며칠 걸림?
```

**이게 없으면** Agent가 "무엇을 먼저 조치할지" 순서를 못 정합니다.

---

## C-3. `Micro_Crack / Cooling_Water_Temp` — 방향이 반대입니다

| | |
|---|---|
| 확정 도메인 | `Cooling_Water_Temp` **증가** → Micro_Crack 증가 |
| 데이터 | Cliff's delta **−0.013** (반대 방향, 크기 0) |
| 경보 검사 | 위험구간 2.41% < 정상구간 2.60% (**0.93배, 역전**) |

**현재 T4(판단보류)**로 두었습니다.

### 관련 관찰 — 냉각 컬럼의 변동 폭이 너무 좁습니다

| 컬럼 | 10분위 전체 범위 |
|---|---|
| `Cooling_Water_Temp` | 21.51 → 22.50 (**약 1℃**) |
| `Cooling_Flow` | 9.79 → 10.16 (**0.37**) |

**여쭤볼 것**: *"이 데이터셋에 실제 Cooling Failure 사건이 포함돼 있습니까?"*

실패 사건이 아예 없다면 **관계가 없는 게 아니라 확인할 데이터가 없는 것**입니다.

---

## C-4. `Micro_Crack` 확정 원인 0건 — 다이싱 단계 컬럼 부재

확정 도메인 3건이 전부 T3/T4입니다.

| 인자 | 티어 | delta |
|---|---|---|
| `Cooling_Flow` | T3 | −0.023 |
| `Vibration` | T3 | +0.063 |
| `Cooling_Water_Temp` | T4 | −0.013 |

**표본 부족이 아닙니다.** r1에서 15,372건을 확보했는데도 모델 성능이 동전던지기 수준입니다.

**여쭤볼 것**: *"블레이드 다이싱 단계를 나타내는 컬럼(블레이드 마모/드레싱/런아웃/회전수)이 있습니까?"*

---

## C-5. 컬럼 정의 확인 4건

| 인자 | 확인 사항 |
|---|---|
| `Surface_Roughness` | 실제 측정값인가, 형식상 컬럼인가? **drop 여부 미확정** |
| `Bottom_Kerf` | 다른 kerf 컬럼과 값이 중복인가? |
| `Cooling_Flow` `Cooling_Water_Temp` | 설비-컬럼 매핑 |
| `Coating_Thickness` | 가공 **전** 측정인가 **후** 측정인가? |

---

## C-6. `Vibration` 세분화 데이터

축별/시점별로 나눈 진동 데이터가 있는지.
→ **레이저 단계 영향과 다이싱 단계 영향을 분리**하기 위함입니다.

---

# D. 기록만 (결정 아님, 그러나 알고 있어야 함)

## D-1. ~~`CLN_Pressure` 트레이드오프~~ — **해소됨 (2026-08-06)**

공정 도메인 확정에 따라 방향을 정정했습니다.

```
이전:  CLN_Pressure 증가 → Particle 증가   ← Remain_Coat와 상충
정정:  CLN_Pressure 증가 → Particle 감소   ← Remain_Coat와 같은 방향
```

| defect | 압력을 올리면 |
|---|---|
| Remain_Coat | **좋아짐** ✅ |
| Particle | **좋아짐** ✅ |

**두 defect 모두 개선되므로 상충이 없습니다.** Agent가 반대편 영향을 병기할 필요가 없어졌습니다.

> ⚠️ 다만 정정 후 **데이터와 방향이 어긋납니다.** Particle의 실측 `delta`는 **+0.012**(양수)로
> 도메인 기대(음수)와 반대입니다. 크기는 사실상 0이라 강한 반박은 아니지만,
> **T4(판단보류)** 로 내려갑니다. 회의에서 확인이 필요합니다.

---

## D-2. `rel_04_domain_knowledge.csv`에 철회된 항목이 남아 있습니다

```
[검증됨|데이터_실증] "Vibration이 Micro_Crack 원인 1위 ..."
```

**`rel_01`에서는 강등했는데 `rel_04`는 안 고쳤습니다. 두 파일이 모순입니다.**
**이 상태로 main에 올라가 있습니다.**

→ 해당 항목을 `[철회됨]`으로 바꾸고 강등 사유를 적어야 합니다.

---

## D-3. `pure` 라벨 정의가 담당자마다 다릅니다

| 담당자 | 정의 |
|---|---|
| JHdaimma (기존 `rel_01`) | **상대 defect만** 제외 |
| Jun / daeho | **전체 defect** 제외 |
| **JHdaimma (`rel_20`)** | **전체 defect 제외** ← 통일함 |

**통일 전까지 `rel_01`과 `rel_20`의 `delta` 직접 비교는 주의**해야 합니다.

---

## D-4. daeho님 Particle 분석에 멘토 제외 지시 컬럼이 포함돼 있습니다

`Focus` · `Cutting_Offset` — 멘토가 제외를 지시한 컬럼입니다.
Particle 결과 재확인 시 **이 두 컬럼을 빼고 재실행**해야 합니다.

---

## D-5. 김시우님 Goal5 반영 필요 6건

`rel_07_health_index_link.csv` 참조. **현재 main의 Agent가 틀린 답을 하고 있습니다.**

| 조치 | 대상 |
|---|---|
| **삭제 2** | Micro_Crack `Vibration` `Cooling_Flow` — 근거 철회됐는데 아직 원인이라고 답함 |
| **역할변경 4** | `Kerf_Width_Profile` `Top_Kerf` `Bottom_Kerf` `Groove_Depth` → 감시지표. **`Groove_Depth`는 측정값이라 조치 자체가 불가능** |

---

# ✅ 다음 회의에서 최소한 이 3개만

1. **A-1** `Particle/CLN_Flow` T2 → T3?
2. **B-1** `Particle/Vibration` — daeho님 결론 유지? 강등?
3. **B-3** 감시지표 트랙을 유지할 것인가 (특히 `Groove_Depth`)

이 3개가 정해지면 나머지는 코드로 반영 가능합니다.

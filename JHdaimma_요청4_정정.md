# 요청 4 정정 — 후보 2쌍 중 1쌍을 철회하고, 남은 1쌍의 근거를 교체합니다

- 2026-08-08 / 김시우 (추세분석·Health Index)
- 원문: `JHdaimma_협의요청.md` 요청 4
- 관련 커밋: `baae73f`

---

## 먼저, 제 쪽 방법에 오류가 있었습니다

요청 4에 올린 두 쌍의 근거를 **R1(학습셋)에서 뽑았습니다.** R1은 불량 시나리오를 의도적으로
주입한 데이터라, 거기서 나온 분리배수는 공정의 성질이 아니라 주입 레시피를 되읽은 값입니다.
같은 스캔을 감시 데이터(원본 100k)로 다시 돌리니 판정이 뒤집힙니다.

Remain_Coat 기준, 신호/노이즈:

| 컬럼 | R1 | 원본 | |
|---|---|---|---|
| Coating_Thickness | 2.69 | **6.47** | R1이 진짜 신호를 묻고 있었음 |
| CLN_Pressure | 2.07 | **6.92** | 이미 채택된 T1인데도 R1에선 약해 보임 |
| **Vibration** | 1.65 | **0.96** | **R1에만 있던 가짜** |
| Bottom_Kerf | 2.63 | 0.96 | 위와 같음 |
| Laser_Centering_Position | 2.21 | 0.94 | 위와 같음 |

R1에서는 18개 컬럼이 2.0~2.7배에 뭉쳐 서로 구분이 안 됩니다. 원본에서는 3개(18.5~20.9배)와
나머지 31개(≤1.0배)로 깨끗하게 갈립니다.

**요청 3에서 제가 지적한 것과 같은 종류의 문제를 저도 하고 있었습니다** — `upper_candidate_p99`가
합본 200k 기준이라 못 쓴다고 말씀드렸으면서, 정작 유형 판정은 R1으로 하고 있었습니다.

---

## 정정 1 — `Vibration ↔ Remain_Coat` : **철회합니다**

원본 기준 분리 2.757배, 순열 천장 3.380배 — **천장 아래**입니다. 방향일관성도 0.559로
(54그룹 중 절반만 같은 방향) 천장 0.744에 못 미칩니다.

요청 4에 적었던 "오경보율 0.809%"는 유효한 측정이지만, 그건 **경보가 안 울린다**는 뜻이지
**관계가 있다**는 뜻이 아니었습니다. 판정 요청 대상이 아닙니다. 검토 시간 쓰셨다면 죄송합니다.

---

## 정정 2 — `Coating_Thickness ↔ Remain_Coat` : **근거가 훨씬 강해졌습니다**

요청 4에서 "2.69배, 다만 드리프트 장비가 없어 정탐 미검증"이라고 올렸던 쌍입니다.
원본 기준으로는 이미 채택된 두 원인과 **같은 급**입니다.

| 인자 | 분리배수(원본) | 방향일관성 | 학습된 그룹 | 상태 |
|---|---|---|---|---|
| CLN_Flow | 20.93 | 1.000 | 52/54 | T1 채택됨 |
| CLN_Pressure | 19.81 | 1.000 | 54/54 | T1 채택됨 |
| **Coating_Thickness** | **18.52** | **1.000** | **54/54** | **미평가** |
| (4위) Process_Time | 2.86 | 0.500 | 40/54 | 천장(3.38) 아래 |

54개 그룹 전부가 `low_is_risky`로 일치하고, 4위와의 격차가 6.5배입니다.

### 이게 왜 중요한가 — 경계가 멘토 스펙 **안쪽**에 있습니다

학습된 경계는 **9.699**, 멘토 LSL은 **9.5**입니다.

| 구간 | 샷 수 | Remain_Coat 발생률 |
|---|---|---|
| 위험구간 전체 (< 9.699) | 2,263 | **29.92%** |
| 그중 스펙 안 (9.5 ~ 9.699) | **2,218 (98.0%)** | **29.85%** |
| 정상 (≥ 9.699) | 97,737 | 1.69% |

**위험구간의 98%가 스펙을 만족합니다.** 스펙 위반은 100,000샷 중 45건뿐입니다. 즉 스펙 감시
(LSL/USL)로는 이 구간을 원리적으로 못 잡습니다.

4대 전부 같은 그림입니다 — 진입률 2.19~2.33%, 구간내 발생률 26.5~31.9%. CLN_Flow처럼
특정 장비에만 나타나는 패턴이 아닙니다.

### 커버리지

| 감시 조합 | 잡히는 Remain_Coat |
|---|---|
| 현재(CLN_Flow + CLN_Pressure) | 1,617 / 2,332 = **69.3%** |
| + Coating_Thickness | 2,210 / 2,332 = **94.8%** |

Coating_Thickness 단독 기여는 **593건(25.4%)**, 샷 2,091개(전체의 2.1%)에서 나옵니다.
그 구간의 발생률은 28.4%입니다.

### 정탐 검증 건 — 요청 4의 단서를 취소합니다

요청 4에서 "드리프트 장비가 없어 정탐이 검증 안 됐다"고 단서를 달았는데, 그건 R1 기준
분석의 결과였습니다. 원본에서는 4대 모두 이 구간에 실제로 들어가고 들어갈 때마다 발생률이
17.7배로 뜁니다. 정탐은 검증됐습니다.

### 필요한 것

`Coating_Thickness ↔ Remain_Coat`가 tier 체계를 통과하는지 판정 부탁드립니다.
통과하면 tier·방향(`low_is_risky`)·`alert_usable`을 `agent_cause_factors.json`에 넣어주시면
저희가 B유형 → C유형으로 옮기겠습니다.

저희가 임의로 안 넣는 이유는 요청 4와 같습니다 — 짝짓기의 단일 출처는 관계DB입니다.
현재 `rel_20_tier_table.csv`의 Remain_Coat 행은 CLN_Pressure/CLN_Flow 둘뿐이라, 이 쌍은
기각된 게 아니라 평가 대상이 아니었던 것으로 이해하고 있습니다.

---

## 참고 — Particle 후보 3건은 저희 쪽에서 기각했습니다

같은 스캔에서 Particle 쪽으로도 3건이 천장을 넘었는데, 전부 교란이었습니다.
Remain_Coat를 통제하고 재검정한 결과입니다.

| 쌍 | 전체 | Remain_Coat=0 한정 | 변화 | 천장 2.332 |
|---|---|---|---|---|
| CLN_Flow → Particle | 3.526 | 2.231 | −36.7% | 미달 |
| Coating_Thickness → Particle | 3.088 | 2.114 | −31.5% | 미달 |
| CLN_Pressure → Particle | 2.736 | 1.836 | −32.9% | 미달 |
| **(대조) Surface_Roughness → Particle** | 17.741 | **259.679** | **+1364%** | 통과 |

경계값이 Remain_Coat 쪽과 거의 같았던 게 단서였습니다(Coating_Thickness: Particle 9.709 vs
Remain_Coat 9.699). 같은 사건을 두 번 세고 있었습니다.

**`CLN_Flow ↔ Particle`을 `alert_usable=False`로 두신 판정과 같은 결론입니다.** 서로 다른
방법으로 같은 답이 나온 게 이번이 세 번째입니다.

---

## 저희 쪽 방법 변경 (참고)

판정 컷을 두 개 다 순열검정에서 뽑도록 바꿨습니다. 사람이 고른 상수를 없앴습니다.

1. **분리배수 천장** — Westfall-Young 최댓값 통계량. 34개 컬럼을 동시에 훑으므로 p95를 그대로
   쓰면 컬럼당 5%씩 오탐이 쌓입니다(기대 1.7개). 순열 1회마다 "그 순열에서 가장 잘 갈린
   컬럼"을 기록해 그 분포의 p95를 씁니다. Bonferroni보다 덜 보수적이고 컬럼 간 상관을
   자동 반영합니다.
2. **방향일관성 천장** — 같은 방식. 예전엔 0.8을 손으로 박아뒀는데 실제로 재보니 **0.739**라
   근거 없이 과하게 엄격했습니다.

## 저희 쪽 한계 (참고)

**Chipping(원본 4건) / Micro_Crack(41건) / Laser_Paim(0건)은 이 스캔이 성립하지 않습니다.**
그룹당 최소 표본을 못 채워 스텀프가 아예 학습되지 않아, 결과표에 행 자체가 없습니다.
이 셋의 유형 배정은 데이터로 검증할 수 없고 도메인/관계DB 근거로만 갑니다 —
Micro_Crack에 대해 주신 회신("이 데이터로는 확인할 수 없다")과 같은 상황입니다.

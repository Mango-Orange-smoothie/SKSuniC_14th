# JHdaimma님 회신 — 정정 3건 + 실측 공유

- 2026-08-08 / 김시우 (추세분석 · Health Index)
- 회신하신 문서: `26.08.05_Goal2_통합_Relationship_DB_JHdaimma/김시우님_협의요청_회신.md`
- 확인한 커밋: `origin/JHdaimma` `c6d1d18` (rel_28/rel_30 스키마 + repro_state 라벨 정정)

먼저 — **①②③⑤⑥ 전부 그대로 따르겠습니다.** 스키마 변경도 확인했고 저희 코드는 안 깨집니다
(장비 점수 14.5 / 47.6 / 50.8 / 91.9로 동일). `repro_state` 라벨을 **"실패(도메인 방향과 불일치)"**
로 정정하신 것도 확인했습니다 — 저희도 같은 판단이었습니다.

⑥ `CLN_Pressure` `spike → level`은 저희 코드에 영향 없습니다(`watch_mode`를 읽기만 하고
분기에 안 씁니다). 낙차 p1 −4.82 ~ p99 +4.83에 낙차>2.0이 16.8%라는 근거도 명확합니다.

아래는 **정정할 것 / 확인 부탁드릴 것 / 새로 나온 것**만 적었습니다.

---

## 1. ④ `repro_state` 3값 — 저희가 한 단계 더 갔습니다. 확인 부탁드립니다

> *"판정불가와 실패를 같이 묶지만 않으시면 됩니다."*

동의합니다. 다만 **`판정불가`를 "경보로 분리"가 아니라 "점수 포함"으로 바꿨습니다.**
`b85650f`를 보고 "✅ 지금 하신 것"이라고 하셨는데, 바로 다음 커밋 `bcb837a`에서 바뀌었습니다.

| `repro_state` | JHdaimma 의견 | 저희 현재 |
|---|---|---|
| `통과` | 점수 포함 | 점수 포함 |
| **`판정불가(원본 표본부족)`** | **경보로 분리** | **점수 포함** ← 다름 |
| `실패(...)` | 아예 제외 | 제외 |

### 왜 바꿨나 — 멘토님이 정답을 주셨습니다

2026-08-08에 주입 시나리오 확정본을 받았습니다.

```
DP02 (Laser Aging)      → Power_Efficiency 감소 ↓ Kerf 증가 ↓ Chipping 증가
DP03 (Cooling Failure)  → Head_Temp 상승 → Micro_Crack / Chipping 증가
DP04 (Cleaning Failure) → CLN_Flow 감소 ↓ Remain_Coat / Particle 증가
DP01                    → 시나리오 없음
```

**관계DB의 `domain_direction`이 이 문장과 글자 그대로 같습니다.** 즉 Chipping 짝은
"검증 안 된 관계"가 아니라 **확정된 관계인데 이 데이터셋에 라벨이 안 실린 것**입니다
(감시 데이터 Chipping 4건, Laser_Paim 0건). 그래서 점수에 넣었습니다.

원인 변수 이동량도 3/3 일치합니다 — DP02 `Power_Efficiency −0.56σ → Bottom_Kerf +0.62σ`
(멘토 사슬 그대로), DP03 `Head_Temp +0.57σ`, DP04 `CLN_Flow −1.97σ`, DP01 전 변수 ±0.05σ.

그 결과 **장비 4대의 1순위 불량이 멘토 시나리오와 전부 일치**합니다.

### 확인 부탁드릴 것

**"판정불가"를 점수에 넣는 것에 동의하시는지**입니다. 반대하시면 되돌리겠습니다 —
다만 그러면 DP02·DP03의 1순위가 Chipping이 아니게 되어 멘토 정답과 어긋납니다.

## 2. 제 실측 보고가 틀렸습니다 — "경계 진입 0건"

협의요청 마지막 절에 이렇게 썼습니다:

> *"`threshold_source_dataset = "r1 주도"` 경고도 실측으로 확인했습니다 — 해당 인자들은
> 감시 데이터에서 경계 진입 경보가 **0건**입니다. 주석에 적어두신 그대로였습니다."*

**순환논증이었습니다.** `trend_analysis_results.csv`에서 "threshold 문구가 든 경보 행"을
셌는데, 그 컬럼들은 **C유형이 아니라서 threshold 분기 자체가 안 돕니다.** 제가 내린 제외
결정의 결과를 세놓고 독립 확인이라고 보고한 셈입니다.

### 다시 쟀습니다 — rel_30 경계에 실제로 도달하는 비율

| 인자 | rel_30 경계 | 감시 데이터 진입 | 그 구간의 Chipping (전체 4건) |
|---|---|---|---|
| Head_Temp | 42.646 ↑ | **2.11%** | 0건 |
| Power_Efficiency | 94.742 ↓ | **6.29%** | 0건 |
| **Laser_Power** | **18.380 ↓** | **9.87%** | **4건 (전부)** |
| **Cooling_Flow** | **9.934 ↓** | **25.96%** | 0건 |

Head_Temp는 `known_limitations` 3번에 적어두신 것과 정확히 일치합니다(2,099행 / Chipping 0건 →
저희 계산 2,114행 / 0건).

### 여기서 두 가지가 나왔습니다

**(가) `Laser_Power ↔ Chipping`은 감시 데이터에서도 검정이 됩니다**

저희 6-1 스캔(결정트리 스텀프)은 그룹당 `min_samples_leaf=10`을 요구해서 Chipping 4건에는
아예 안 돕니다. **그런데 "그 4건이 좁은 위험구간 안에 들어가는가"는 이항검정으로 셀 수
있습니다.**

```
위험구간(≤18.380)이 전체의 9.87%인데 Chipping 4건이 전부 그 안에 있습니다.
값: 18.306 / 18.280 / 18.292 / 18.281   (4대에서 하나씩)
무관하다면 확률 0.0987⁴ = 0.0095%  →  p = 9.5e-05
```

멘토님 확정 시나리오(`Laser Aging ↓ Power_Efficiency 감소 ↓ Kerf 증가 ↓ Chipping 증가`)의
마지막 화살표에 해당합니다. **이 짝만 C유형으로 올렸습니다.**

나머지 3개는 **반박이 아니라 정보 없음**입니다 — 무관해도 4건 모두 구간 밖일 확률이
77~92%입니다. 그래서 넷 중 하나만 올렸습니다.

**(나) `Cooling_Flow` 경계는 확인 부탁드립니다**

```
경계 9.934 아래       : 25.96%  (25,960샷)
위험범위 9.72~9.92     : 21.38%  (21,382샷)
두 구간의 Chipping     : 0건
장비별                : DP01 25.9% / DP02 25.7% / DP03 26.8% / DP04 25.5%
```

**정상 생산의 4분의 1이 "위험범위" 안에 있습니다.** 4대가 고르게 나오고
`threshold_source_dataset`도 `"양쪽 기여"`라 r1 단독 문제는 아닌 것 같습니다.

저희는 이 경계를 계산에 안 씁니다(C유형 미채택, Health Index는 관리한계 기준). **다만
agent가 `get_defect_causes("Chipping")`에서 `위험범위 9.72~9.92`를 그대로 인용합니다.**
엔지니어가 보면 "냉각유량을 9.94 위로 올려라"로 읽는데 이미 21%가 그 아래입니다.

값은 `rel_30`이라 저희가 고칠 게 아니라서 실측만 전달드립니다. 판단 부탁드립니다.

> 참고 — 위험구간이 넓은 것 자체는 문제가 아닙니다. 이미 쓰고 있는 Surface_Roughness가
> 33.14%인데 구간내 Particle이 21.0%(전체 7.8%)로 잘 작동합니다. Cooling_Flow는 **넓은데
> 그 안에 Chipping이 없다**는 게 다른 점입니다.

---

## 3. ⑤ Micro_Crack lift 0.39 — 해석이 맞습니다. 증거를 붙입니다

> *"'데이터가 반박했다'가 아니라 '확인할 데이터가 없다'입니다."*

**검정해봤고, 기전까지 확인됩니다.** R1 상한(0.2111) 초과 구간 19,252샷의 NG_Code 분포:

| NG_Code | 초과구간 | 전체 | lift |
|---|---|---|---|
| **CHIP** | **82.33%** | 24.17% | **3.41** |
| **OK** | 10.17% | 58.89% | **0.17** |
| CRACK | 2.15% | 4.89% | 0.44 |
| PARTICLE | 2.62% | 4.84% | 0.54 |
| REM_COAT | 2.44% | 6.93% | 0.35 |

**CHIP을 뺀 전부가 lift < 1이고, OK조차 0.17입니다.** NG_Code가 단일 라벨인데 그 구간의
82%를 CHIP이 차지했으니 나머지는 산술적으로 눌릴 수밖에 없습니다.

**Micro_Crack의 0.39는 관계의 부재가 아니라 라벨 경쟁입니다.** 입장을 바꾸신 게 옳았고,
제안하신 문구 그대로 가겠습니다.

원본 기준 581샷 / Chipping 0건 / Micro_Crack 0건도 정확히 재현됩니다.

---

## 4. 요청 4 정정 — `Coating_Thickness`

관계DB에 없는 게 맞다고 하신 것 따르겠습니다. 다만 **근거 수치를 정정**드립니다 — 제가
올렸던 2.69배가 **R1에서 나온 값**이었습니다.

| | R1 | 감시 데이터 |
|---|---|---|
| Remain_Coat 분리력 | 2.69 | **6.47** (분리배수 18.52 vs 천장 3.38) |
| 방향일관성 | 0.981 | **1.000** (54그룹 전부 `low_is_risky`) |

이미 T1인 CLN_Flow(20.93) / CLN_Pressure(19.81)와 같은 급이고 4위(2.86)와 6.5배 차이입니다.

**경계 9.699가 멘토 LSL 9.5보다 위입니다** — 위험구간 2,263샷 중 **2,218샷(98.0%)이 스펙 안**
이고 그 구간의 Remain_Coat가 29.9%(정상 1.7%)입니다. 스펙 감시로는 원리적으로 못 잡습니다.

### 저희 처리

**원인 지목은 안 하고 경보만 냅니다.** `config.ALARM_ONLY_C_COLUMNS`로 분리했습니다:

- `is_cause_factor = False`라 defect 건강도에 안 들어갑니다
- 경보 문구도 defect 이름을 안 부릅니다
  (`"위험 Threshold(9.699) 구간에 들어감(이 장비 평소 2.16%)"`)

**"이 인자가 그 불량의 원인이다"(인과 지목)와 "값이 불량률이 뛰는 경계를 넘었다"(경계 보고)는
다른 주장**이라고 봤습니다. 앞엣것만 관계DB가 단일 출처여야 하고, 뒤엣것은 감시 데이터에서
저희가 직접 검정할 수 있습니다. 이 구분이 괜찮은지 확인 부탁드립니다.

---

## 5. 저희 쪽 큰 변경 — 참고

### threshold를 감시 데이터에서 배우기 시작했습니다

R1이 필요한 이유는 감시 데이터의 Chipping 4건 / Micro_Crack 41건인데, **C유형 짝이 전부
Remain_Coat(2,332) / Particle(7,792)이라 R1이 필요 없었습니다.** 그런데도 R1으로 배우다가
Coating_Thickness에서 사고가 났습니다 — 54그룹 중 1그룹만 방향이 뒤집혀(53 low / 1 high, 그
1그룹 경계가 10.11) Health Index가 컬럼을 통째로 버렸습니다.

```
Coating_Thickness   R1 53/54 low, 경계 9.6993
                    원본 54/54 low, 경계 9.6990   ← 값은 거의 같은데 방향이 정리됨
```

이제 짝지어진 defect가 감시 데이터에 충분하면 감시 데이터로 배우고, 아니면 R1으로 폴백합니다.
현재 R1을 쓰는 건 `Laser_Power ↔ Chipping` 하나입니다.

### Health Index 점수 기준선이 바뀌었습니다

컬럼마다 다르던 기준선(멘토 스펙 / 임시 백분위 / 이항 경보선)이 σ 단위로 1000배까지 차이나서
서로 비교가 안 됐습니다. **Shewhart 관리한계(정상값 ± 3σ) 하나로 통일**했습니다.

```
DP04 14.5 / DP02 47.6 / DP03 50.8 / DP01 91.9
```

**"10점 미만 = 관리한계 초과"이고 "스펙아웃"이 아닙니다** — 89일 동안 멘토 스펙 위반 지속
경보는 0건입니다.

---

## 6. `rel_28` 상한 알람을 구현했습니다

저희가 산출해 넘긴 0.2111인데 정작 코드에서 안 읽고 있었습니다. `rel_28`을 읽어
`spec_breach_rule` 그대로 판정합니다 — **"그날 상한 초과 샷 수 ≥ `binom.ppf(0.99, 그날 샷수,
0.001)`"**. 개별 샷 하나가 넘는 건 정상 꼬리라 무시하고 하루치가 몰릴 때만 경보합니다.

문구는 말씀하신 대로 **"관련 가능성"**입니다:

```
DP01의 Vibration이(가) 설비 상한(0.2111)을 넘었습니다(현재 0.2192).
그날 초과 샷이 우연으로 보기 어려운 수준입니다 — 설비 정비 점검 필요.
Chipping 관련 가능성 / Micro_Crack 관련 가능성.
```

`evidence_level` 열 하나로 문구가 나오게 만들어주신 것 잘 썼습니다.

### 적용 결과 — Vibration 상승 순서와 정확히 일치합니다

| 장비 | Vibration σ 이동 | 상한 초과 비율 | **경보 일수(89일 중)** |
|---|---|---|---|
| DP03 | 1.02σ | 1.38% | **46일** |
| DP02 | 0.49σ | 0.49% | **31일** |
| DP04 | 0.53σ | 0.32% | **23일** |
| **DP01 (멘토 확정 정상)** | **0.02σ** | 0.13% | **4일** |

> 이전에 제가 "DP02 19일 · DP03 43일 · DP01 1일"이라고 말씀드렸는데, 그건 OK샷만으로
> 셌던 값입니다. `spec_breach_rule` 그대로 전 샷 기준으로 재면 위 표가 맞습니다.

**DP04가 23일 나오는 건 오탐이 아닙니다** — DP04의 Vibration도 실제로 0.53σ 올라갑니다
(멘토 시나리오는 Cleaning Failure지만 진동도 같이 오르는 것으로 보입니다).

### 주의 — 아직 잠들어 있습니다

`origin/JHdaimma`가 main에 병합되기 전이라, main 기준으로 돌리면 이렇게 나옵니다:

```
[관계DB] 경고: rel_28에 upper_limit 열이 없습니다(옛 스키마) — 상한 알람을 건너뜁니다.
```

조용히 빠지지 않게 경고를 찍도록 했습니다. **브랜치 병합해주시면 바로 켜집니다.**

---

## 요약

| | |
|---|---|
| ④ `repro_state` | 3값 구분 동의. **다만 "판정불가"를 점수에 포함하도록 한 단계 더 갔습니다** — 멘토 정답 근거. 동의 여부 확인 부탁 |
| **제 오류** | **"경계 진입 0건"은 순환논증.** 실제 2.11~25.96% |
| **새로 나온 것** | `Laser_Power ↔ Chipping` 이항검정 통과(4/4, p=9.5e-05) → C유형 승격 |
| **확인 요청** | `Cooling_Flow` 경계 9.934 — 정상 생산의 26%가 그 아래 |
| ⑤ | 해석 맞습니다. lift 0.39는 CHIP이 82%를 차지한 라벨 경쟁 |
| 요청4 | Coating_Thickness 근거를 감시 데이터 수치로 정정. 경보 전용으로 처리 |
| **rel_28** | **상한 알람 구현 완료.** DP03 46일 / DP02 31일 / DP04 23일 / DP01 4일 — **브랜치 병합 대기 중** |

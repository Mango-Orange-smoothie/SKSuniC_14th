# rel_28_vibration_alarm.csv 기준값 회신

- **회신자**: 김시우 (추세분석)
- **일자**: 2026-08-08
- **대상**: `26.08.05_Goal2_통합_Relationship_DB_JHdaimma/rel_28_vibration_alarm.csv`
- **요청 근거**: 해당 파일의 `alarm_owner = "추세분석 담당"`, `status = "스키마만 정의 — 기준값은 추세분석 담당이 채움"`

---

## 1. 먼저 확인할 것 — 제시된 상한 후보는 쓸 수 없습니다

`upper_candidate_p99 = 0.2609`, `upper_candidate_p999 = 0.2841`은 **합본(원본 100k + R1 100k) 200,000행 기준**입니다.
(`defect_rate_overall_pct = 6.234`가 합본의 pure Chipping 비율 6.234%와 정확히 일치하는 것으로 확인)

| 데이터셋 | p99 | p999 | **최댓값** |
|---|---|---|---|
| 감시(원본 `DP_HealthIndex_Dataset.csv`) | 0.2066 | 0.2241 | **0.2487** |
| R1 | 0.2687 | 0.2895 | 0.3102 |
| 합본 200k | **0.2609** | **0.2841** | 0.3102 |

**감시 데이터의 최댓값(0.2487)이 후보 상한(0.2609)보다 낮습니다.** 이 값을 그대로 쓰면 경보가 영원히 울리지 않습니다.
`rel_20_tier_table.csv`의 `threshold_source_dataset = "r1 주도(원본 기여 미미)"` 항목들과 같은 문제입니다.

같은 파일의 `upper_original_p99 = 0.2066`은 감시 데이터 기준이라 쓸 수 있으나, **89일 전체로 계산해 열화 구간이 섞여 있습니다**(DP02·DP03·DP04가 상승 추세). 그래서 아래와 같이 다시 산출했습니다.

---

## 2. 산출 방법

**안정 구간(Phase I) × OK샷**만 사용했습니다.

- 안정 구간: 일별 Vibration 평균 계열에 Mann-Kendall을 확장창으로 반복 적용해, 상승 추세가 유의(p<0.05)해지기 **직전까지**
- OK샷만: 불량 샷은 정의상 위험 쪽에 몰려 있어 기준을 밀어냄

| 장비 | 안정 구간 | OK샷 | p99 | p99.9 |
|---|---|---|---|---|
| DP01 | 89일 | 22,662 | 0.1958 | 0.2115 |
| DP02 | 13일 | 3,268 | 0.1962 | 0.2110 |
| DP03 | 41일 | 10,570 | 0.1955 | 0.2089 |
| DP04 | 51일 | 13,306 | 0.1965 | 0.2117 |
| **풀링** | — | **49,806** | **0.1960** | **0.2111** |

4대의 값이 p99 0.1955~0.1965, p99.9 0.2089~0.2117로 **수렴합니다.** 동일 사양 장비이므로 이것이 맞는 그림이며, 89일 전체로 계산하면 열화 때문에 장비별로 벌어집니다.

---

## 3. 채울 값

### `spec_breach_rule`

```
상한 = 0.2111  (안정구간 OK샷 p99.9, 감시 데이터 기준)

경보 = (그날 상한 초과 샷 수) >= binom.ppf(0.99, 그날 샷 수, 0.001)
       → 하루 250~300샷 기준 초과 3샷 이상
```

고정 배수나 고정 개수가 아니라 **이항분포 꼬리확률**을 쓰는 이유: 하루 샷 수가 날마다 다르고, 고정 기준은 샷 수에 따라 엄격도가 달라집니다. 이 방식은 어느 날이든 오탐 확률이 1% 이하로 고정됩니다. (추세분석 파이프라인의 C유형 판정도 26.08.08부터 같은 방식으로 통일했습니다.)

### `trend_window_days` — **해당 없음**

추세분석 구현은 고정 창이 아니라 **CUSUM 누적합**을 씁니다. 창 길이 대신 아래 두 상수가 파라미터입니다.

| 상수 | 값 | 의미 |
|---|---|---|
| `CUSUM_K` | 0.7σ | 이보다 작은 편차는 누적하지 않음(정상 노이즈) |
| `CUSUM_H` | 4.5σ | 누적합이 이를 넘으면 "지속적 이동" 확정 |

### `trend_slope_threshold` — **미사용**

기울기 임계값이 아니라 **Mann-Kendall p<0.05**로 판정합니다. 참고용 실측 OLS 기울기(일당):

| 장비 | kendall tau | p-value | OLS 기울기/일 | 판정 |
|---|---|---|---|---|
| DP01 | 0.080 | 0.266 | 0.00031 | no_trend |
| DP02 | 0.716 | 2.9e-23 | 0.00771 | upward_drift |
| DP03 | 0.716 | 2.9e-23 | 0.01794 | upward_drift |
| DP04 | 0.624 | 5.0e-18 | 0.00836 | upward_drift |

---

## 4. 적용 결과 (감시 데이터 89일)

| 장비 | MK 판정 | 실제 상승 시작 | **급이탈 경보** | 기존 추세경보(CUSUM) |
|---|---|---|---|---|
| DP01 | no_trend | — | 1/89일 (오탐) | — |
| DP02 | upward | ~1/21 | **1/29** (19일) | 2/09 (170건) |
| DP03 | upward | ~2/01 | **2/13** (43일) | 2/18 (8,525건) |
| DP04 | upward | ~2/10 | 3/06 (7일) | 2/28 (75건) |

- **DP02는 11일, DP03은 5일 빨리 잡습니다** — 급이탈 경보가 추세경보를 앞섭니다
- DP04만 6일 늦습니다
- 추세 없는 DP01에서 89일 중 1일 오탐 (1.1%)

더 민감한 후보(p99 = 0.1960)도 검토했으나, DP02를 상승 시작일에 바로 잡는 대신 **DP04에서 18일 이른 오탐**이 발생해 채택하지 않았습니다. Vibration은 `role = 원인(정비대상)`이라 오경보가 불필요한 설비 분해로 이어질 수 있어 보수적으로 잡았습니다.

---

## 5. 참고 — `alarm_type` 절반은 이미 구현돼 있습니다

`alarm_type = "추세 상승 + 상하한 급이탈"` 중 **"추세 상승"은 현재 추세분석 파이프라인에서 이미 동작 중**입니다(Vibration은 A유형, CUSUM 기반, 총 8,856건).

이번 회신은 **"상하한 급이탈"** 부분의 기준값입니다. 코드 반영은 아직 하지 않았고, DB 갱신 후 진행 예정입니다.

## 6. 확인 요청

1. 상한 **0.2111** 채택 여부 (합본 기준 0.2609 대신 감시 데이터 안정구간 기준)
2. `trend_window_days` / `trend_slope_threshold` 필드를 **"해당 없음(CUSUM 방식)"** 으로 남겨도 되는지, 아니면 스키마를 CUSUM 파라미터에 맞게 바꿀지
3. `defect` 짝은 **Chipping** (lift 4.82). 단 감시 데이터의 pure Chipping은 3건뿐이라 `rel_20`의 `repro_state` 논리대로면 재현 검증이 불가능합니다. 도메인 근거(`domain_evidence = 멘토 확정`)로 진행하는 것이 맞는지 확인 부탁드립니다.

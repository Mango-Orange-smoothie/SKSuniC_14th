# 공정품질 통합관리 AI Agent — 프로젝트 설명

작성 2026-08-08 / 김시우 (전처리·추세분석·Health Index 담당)

---

## 0. 한 줄 요약

> **스펙 안에서, 샷 노이즈의 절반만큼 움직이는 변화를 두 달 전에 잡는다.**

---

## 1. 문제 정의 — 스펙 검사로는 안 보인다

HBM 다이싱 공정 데이터 100,000샷(4대 × 89일)을 분석한 결과입니다.

| 검사 방법 | 열화 중인 DP02·DP03을 구분할 수 있나 |
|---|---|
| 스펙 위반 판정 | ❌ 해당 변수 위반 **0건** |
| Cpk (공정능력지수) | ❌ 열화 장비 2.62 vs 정상 장비 2.90 — 사실상 동일 |
| 육안 확인 | ❌ 이동 폭이 **샷 표준편차의 0.57배**(중앙값) |

**확인된 열화 11건 중 9건이 1σ 미만으로 움직입니다.** 스펙폭 대비로는 1.7~4.2%입니다.

그런데 실제로는 DP02에서 Laser_Power가 56일째, DP03에서 Head_Temp가 42일째 한 방향으로 밀리고 있습니다. **이걸 잡으려면 "선을 넘었나"가 아니라 "평소와 다른가"를 통계적으로 누적해서 봐야 합니다.**

---

## 2. 전체 구조

```
  원본 데이터 (100,000행 × 68컬럼)
        │
        ▼
  ┌─────────────────────────────────────────┐
  │  Step 0: 전처리        pipeline/         │   6단계
  │  → 컬럼 분류 / 변동성 / baseline / 추세  │
  └─────────────────────────────────────────┘
        │  00_*.csv (13개 산출물)
        ▼
  ┌─────────────────────────────────────────┐
  │  추세분석             trend_analysis.py  │   경보 판정
  │  → early_warning, episode, 지속일        │
  └─────────────────────────────────────────┘
        │  trend_analysis_results.csv
        ▼
  ┌─────────────────────────────────────────┐
  │  Health Index      build_health_index.py │   0~100 점수화
  │  → 장비/불량/변수 3단계 집계             │
  └─────────────────────────────────────────┘
        │  health_index_data.json
        ▼
  ┌──────────────────┐   ┌──────────────────┐
  │  AI Agent        │   │  n8n 자동 알림    │
  │  agent.py        │   │  (일일 메일)      │
  └──────────────────┘   └──────────────────┘
```

**옆에서 들어오는 두 입력**

- `pipeline/mentor.py` — 멘토가 준 스펙(LSL/TARGET/USL 10개 변수), 제외 지정 변수/불량
- `26.08.05_Goal2_통합_Relationship_DB_JHdaimma/` — 팀의 인자↔불량 관계 판정 (누가 어떤 불량의 원인인지)

---

## 3. Step 0 — 데이터 전처리 (6단계)

`pipeline/step0_preprocessing.py`. 6개 목표(Goal 1~6)가 **공통으로 딛고 설 기반**을 만듭니다. 원본은 절대 수정하지 않습니다.

### 1단계 — 로드 & 검증

행 수, 키 중복(Lot_ID+Strip_ID), 정상군 수를 `assert`로 검증합니다. 데이터가 바뀌면 조용히 넘어가지 않고 멈춥니다.

### 2단계 — 변동성 지표

"이 변수는 원래 얼마나 흔들리는가"를 잽니다. 데이터 타입마다 다르게:

| 타입 | 지표 | 이유 |
|---|---|---|
| 연속형 | CV(변동계수) = 표준편차/평균 | 스케일 무관하게 비교 가능 |
| 0 근처 연속형 | 표준편차 / 관측범위 | CV가 발산함 (Cutting_Offset 등) |
| 이진 불량 | 층별 발생률의 CV | 분산 p(1−p)는 무의미 |

### 3단계 — 노이즈 vs 실제 열화 추세

핵심 단계입니다. **운전조건(Product × Recipe)별로 정규화한 뒤** 장비별 일평균 시계열을 만들고, 추세를 검정합니다.

정규화가 필요한 이유: 제품·레시피가 날마다 바뀌므로, 그냥 원값을 보면 "레시피가 바뀐 것"과 "장비가 나빠진 것"이 섞입니다.

**사용 기법 — Mann-Kendall + OLS**

| | 역할 | 원리 |
|---|---|---|
| **Mann-Kendall** | 추세가 **있는지** 판정 | 모든 날짜 쌍을 비교해 "올라간 쌍 − 내려간 쌍"을 셈. 값의 크기가 아니라 **순서만** 보므로 이상치에 강함 |
| **OLS** | 추세의 **크기** 측정 | 최소제곱 직선의 기울기 = 하루당 변화량 |

MK가 판정하고 OLS가 크기를 잽니다. 유의수준 p < 0.05.

> 왜 MK인가: 실측으로 이상치 1개를 넣었을 때 OLS 기울기는 1.20 → 18.20으로 15배 뛰었지만 MK의 tau는 0.80으로 변하지 않았습니다. 공정 데이터에는 튀는 값이 늘 있으므로 판정은 MK가 맡습니다.

### 4단계 — 센서 이상 탐지

결측, 물리적으로 불가능한 0값, flatline(값이 고착돼 센서가 죽은 것)을 플래그합니다.

### 5단계 — 컬럼 분류 taxonomy

68개 컬럼 각각에 서브시스템(레이저/냉각/세정/절단…), 데이터 타입, 분석 역할(예측변수/응답변수/식별자), 변동성 등급, 추세 유무를 붙입니다.

### 6단계 — Baseline 유형 배정 (A/B/C/E)

**"정상이란 무엇인가"를 변수 성격에 따라 다르게 정의합니다.** 이게 이 프로젝트의 설계 핵심입니다.

| 유형 | 개수 | "정상"의 정의 | 예시 |
|---|---|---|---|
| **A** | 8 | 한 방향으로만 나빠짐 — 정상군 median | Head_Temp(↑위험), Vibration(↑), Cooling_Flow(↓) |
| **B** | 14 | 최적 구간이 있음(U자형) — 양방향 다 위험 | Laser_Power, Power_Efficiency, Focus, Feed_Speed |
| **C** | 3 | **불량 데이터로 학습한 위험 경계** | CLN_Pressure, CLN_Flow, Surface_Roughness |
| **E** | 9 | 공정 설계상 이론값(대칭성/정렬) | Cutting_X_Index(=0), Kerf_Angle(=90°), Package_Size(=5.0) |

**C유형이 특별한 이유**: 다른 유형은 "정상 분포에서 얼마나 벗어났나"를 보지만, C유형은 **실제 불량 라벨을 써서 "여기를 넘으면 불량률이 뛴다"는 지점을 학습**합니다.

방법: Product×Recipe 그룹마다 **깊이 1짜리 결정트리(stump)**를 (변수값 → 불량 여부)로 학습해 분할점 하나를 찾습니다. `class_weight="balanced"`로 소수 클래스(불량 2~8%)에 가중치를 줍니다.

> 예시: CLN_Flow가 9.694 아래로 내려간 샷의 Remain_Coat 불량률이 위쪽의 **24.7배**

---

## 4. 추세분석 — 어떻게 경보를 내는가

`trend_analysis.py`. **경보 경로가 4개**이고, 하나라도 걸리면 경보입니다.

### ① CUSUM — 평균이 서서히 밀리는 것

가장 중요한 경로입니다. 매 샷마다 `(현재값 − 정상값)`을 표준편차로 나눈 값에서 **허용치 K를 뺀 나머지를 누적**합니다.

```
누적합 = max(0, 이전 누적합 + z − K)          K = 0.7σ,  H = 4.5σ
누적합 > H  →  경보
```

**작은 편차(0.7σ 미만)는 버리고, 그보다 큰 편차가 지속되면 누적돼서 결국 터집니다.** 한 번 튄 값에는 반응하지 않고 방향성 있는 이동만 잡는 구조입니다.

> **왜 이게 필요한가**: Laser_Power는 스펙폭의 4.2%만 움직였습니다. 어떤 고정 선을 그어도 안 걸립니다. 하지만 그 작은 편차가 56일간 한 방향으로 쌓이면 CUSUM은 반드시 터집니다.

### ② 위험 경계 진입 / 접근 — 꼬리가 위험구간에 들어가는 것

C유형 학습 경계를 쓰는 경로입니다. 두 가지를 봅니다.

- **진입**: 값이 경계를 넘고 그 상태가 지속됨
- **접근**: 아직 안 넘었지만 최근 10샷 중 위험구간 진입 샷이 **우연이라 보기 어려울 만큼** 많음

"우연이라 보기 어려운" 기준은 **이항분포 꼬리확률 1% 미만**입니다(아래 6절).

### ③ 스펙 위반 — 멘토 스펙 10개 변수

`r`샷 연속으로 스펙을 벗어나면 경보. `r`은 변수마다 데이터에서 계산합니다(우연히 그럴 확률 < 1%가 되는 최소 연속 수). 9개 변수를 검사하고(`Focus`는 멘토가 제외), 89일간 **지속 위반은 0건**입니다 — 이 데이터의 스펙 이탈은 전부 단발성 꼬리입니다.

### ④ 설비 상한 — 정비 대상 변수

`Vibration`은 "값을 조정해서 고치는 인자"가 아니라 설비 정비 대상이라 원인 티어표에 없고, ①~③ 어디에도 안 걸립니다. 관계DB(`rel_28`)가 상한을 별도로 넘겨주고, **그날 초과 샷이 우연이라 보기 어려울 만큼 몰릴 때만** 경보합니다. 유일하게 날짜 단위 판정입니다.

> **변동성 확대 경보는 26.08.17에 뺐습니다.** 평균은 그대로인데 산포만 커지는 이상을 잡으려던 경로였는데, 이 데이터에 산포 증가가 없습니다(장비 4대 × 35변수 = 140조합 전부 초반 대비 후반 표준편차가 1.03배 이내). 그런데 **89일 중 86일, 4대 전 변수에서 울렸습니다** — 정상 장비 DP01이 고장 장비 DP04보다 더 많이 울렸습니다. 원인은 10샷 표본 표준편차의 추정 노이즈이고, rolling 창이 9개 샷을 공유해서 그게 이어집니다. **산포 감시가 필요하면 겹치지 않는 부분군에 S 관리도를 붙여야 합니다**(부분군 10이면 상한 B4 = 1.716). 자세한 근거는 `trend_analysis.py` 상단 주석.

### 경보는 "사건"으로 관리합니다

경보 행이 51,039건이지만 **독립 사건(episode)은 5,929건**이고, 화면에 뜨는 건 **44건**(장비×변수)입니다. 그중 점수를 최대폭까지 깎은 `full`은 **9건**이고 나머지 35건은 `early`입니다.

매 샷 알림이 아니라 **"DP02 Laser_Power, 2/2부터 56일째"** 형태의 상태로 관리합니다. 알림 피로를 막기 위해서입니다.

---

## 5. Health Index — 0~100 점수화

`build_health_index.py`. **레벨(지금 얼마나 나쁜가)과 추세(얼마나 오래 나빠지고 있나)를 합칩니다.**

### 5-1. 레벨 — 관리한계까지 여유를 얼마나 썼나

```
margin = (현재값 − 정상값) / (관리한계 − 정상값) × 100
관리한계 = 정상값 ± 3σ        (Shewhart)
```

**전 컬럼 기준선이 하나입니다.** 예전엔 컬럼마다 달랐는데(멘토 스펙 / 임시 백분위 /
이항 경보선), 그 경계들이 정상값에서 떨어진 거리가 σ 단위로 1000배까지 차이나서
서로 비교가 안 됐습니다 — 멘토 스펙 컬럼은 최대 8.6%에 갇히고 진입률 컬럼은 2426%까지
치솟아, 최솟값을 고르면 **4대 전부 진입률 컬럼이 주범**이 됐습니다.

**최근 7일 중앙값**으로 잽니다. 하루치만 보면 노이즈에 흔들립니다(6절 참고).

> **3σ를 고른 이유와 "X-bar 한계는 ±3σ/√n 아닌가" 같은 예상 질문은
> `docs/발표_왜_이_식인가.md`에 따로 정리했습니다.**

### 5-2. 점수 변환

```
margin 0%   →  100점
margin 100% →   10점      ← 관리한계 도달
margin 100% 초과 → 10 × (100/margin) 으로 0에 점근
```

**하위 10점을 "관리한계 초과" 전용 구간**으로 씁니다(`ALARM_BAND`). 이렇게 하면 선을
살짝 넘은 것과 몇 배로 폭주한 것이 둘 다 "0점"으로 뭉개지지 않고 순위가 갈립니다.

> **"10점 미만 = 스펙아웃"이 아닙니다.** 89일 동안 스펙 위반 지속 경보는 0건이고,
> 스펙아웃은 `spec_status`로 따로 표시합니다(현재 전 장비 OK).

### 5-3. 추세 페널티

경보가 확정되면 레벨 점수를 깎습니다. **두 축 중 큰 쪽**을 씁니다.

```
성숙도 = min(1, 경보 지속일 / 14)        증거가 쌓였나
긴급도 = min(1, 14 / 도달 예상일)        곧 넘나
점수   = 10 + (레벨 − 10) × (1 − 0.45 × max(성숙도, 긴급도))
```

두 축의 순위상관이 **0.077로 사실상 무관**하고 활성 경보의 40%는 기울기가 음수라,
어느 하나로 대체하면 반대쪽을 잃습니다. **둘 다 약할 때만 안 깎습니다.**

`레벨 − 10`에만 곱하는 이유: 점수 전체에 배율을 곱하면 관리한계 안인 변수가 "초과 전용
구간"으로 내려가 읽는 법이 깨집니다.

### 5-4. 3단계 집계

```
변수별 점수  →  불량별 점수  →  장비 점수
              (최솟값)        (최솟값)
```

평균이 아니라 **최솟값**을 쓰는 이유: 여러 원인변수 중 하나라도 심각하면 그 불량은
위험합니다. 평균을 내면 나쁜 하나가 좋은 여럿에 희석됩니다. **그리고 가중치를 정할
여지가 없어서 "왜 이 변수가 0.3인가"를 방어할 필요가 없습니다.**

**장비 대표 점수에는 방향이 반박되지 않은 짝만** 넣습니다 — Micro_Crack은 원인 2개가
원본에서 도메인 기대와 반대라 제외됩니다(건강도는 계속 산출되고 경보로 나갑니다).

### 결과

| 장비 | 점수 | 1순위 불량 | 주범 | 목표값에서 | 14일+ 경보 |
|---|---|---|---|---|---|
| **DP04** | **14.5** | Remain_Coat | CLN_Flow | 2.72σ | 1 |
| **DP02** | **47.6** | Chipping | Laser_Power | 0.72σ | 6 |
| **DP03** | **50.8** | Chipping | Head_Temp | 0.53σ | 4 |
| **DP01** | **91.9** | Particle | Surface_Roughness | 0.01σ | 0 |

**멘토 확정 정답(DP02 Laser Aging / DP03 Cooling Failure / DP04 Cleaning Failure /
DP01 시나리오 없음)과 4/4 일치합니다.** 정답을 받기 전에 나온 결과입니다.

---

## 6. 설계 원칙 — 판정 기준을 어떻게 정했나

이 프로젝트에서 가장 신경 쓴 부분입니다. **"이 숫자는 어디서 나왔는가"에 답할 수 없는 값은 없애는 것**이 원칙이었습니다.

### 원칙 1 — 배수가 아니라 확률을 고정한다

예전엔 "평소의 2배면 경보"였습니다. 문제는 **같은 2배가 변수마다 전혀 다른 엄격도**라는 것입니다.

| 변수 | 평소 진입률 | 2배 규칙이 우연히 통과할 확률 |
|---|---|---|
| CLN_Flow | 0.5% | 4.7% |
| CLN_Pressure | 6.4% | **13.0%** |
| Surface_Roughness | 32.0% | 1.5% |

**변수 간 8.7배 차이**입니다. 그래서 배수 대신 **"평소 상태에서 이만큼 나올 확률 < 1%"**로 통일했습니다(`C_DANGER_ALPHA = 0.01`). 오탐 확률 편차가 **29배 → 1.5배**로 줄었습니다.

### 원칙 2 — "평소"는 안정 구간에서 잰다 (SPC Phase I)

기준선을 89일 전체로 재면 **열화 기간이 기준선에 섞입니다.** 그러면 나빠질수록 기준이 같이 올라가 경보가 안 뜨는 자기모순이 생깁니다.

Mann-Kendall로 "아직 추세가 없는 초기 구간"을 자동으로 찾고, 그 구간의 **정상 샷만** 써서 기준을 잡습니다.

> 실제 사례: DP04 CLN_Flow의 기존 기준선 1.72%는 **그 자체가 3월의 열화**였습니다. 2/27 이전 50일간 14,200샷 중 위험구간 진입이 0건이었으니 진짜 기준은 0.03%입니다.

### 원칙 3 — 유형이 경보 방식을 정하지 않는다

예전엔 A/B/E만 CUSUM을 받고 C유형은 경계 판정만 받았습니다. 유형 배정이 틀리면 경보가 죽는 구조였습니다.

전 변수에 **두 경로를 다 적용**합니다. 근거는 35개 × 4대 전수 측정 — 추세 없는 장비에서의 오경보율이 0.000~0.364%로 전부 기준(1%) 미만이라, 붙여서 해로운 변수가 없음을 확인했습니다.

여기에 **감시 전용 10개**(`PLC_CPU_Load`, `Room_Noise`, `Fan_RPM`, `Maintenance_Count` 등 인프라·환경 지표)를 더해 **총 45개**에 target과 관리한계를 붙였습니다. 공정 물리량이 아니라 원인으로는 못 쓰지만, "값이 평소와 다르다"는 말까지 못 할 이유는 없기 때문입니다. 원인 후보 스캔에서는 제외해서 유효인자로 승격될 경로 자체를 막았습니다.

> 검증: 이 10개에서 경보 **0건**. 무관한 지표를 넣어도 오경보가 안 난다는 게 관리한계가 제대로 잡혔다는 방증입니다.

> 효과: CLN_Flow DP04에서 CUSUM이 경계 판정보다 **9일 빨랐습니다**(2/19 vs 2/28).

### 원칙 4 — 팀 판정을 단일 출처에서 읽는다

인자↔불량 짝짓기를 코드에 손으로 적지 않고 Goal2 관계DB에서 읽습니다. 채택 조건은 **`alert_usable=True` AND `repro_state=통과`**입니다.

읽지 않아서 생긴 실제 문제: DB가 "이 짝으로는 경보를 걸면 안 됨"이라고 적어둔 연결(CLN_Flow↔Particle, 위험구간 불량률이 정상구간보다 **낮음**)을 쓰고 있었고, 그 결과 가장 흔한 불량인 Particle이 **감시되지 않으면서 100점으로 표시**됐습니다.

---

## 7. AI Agent

`agent.py`. Claude가 도구를 호출해 답하는 구조입니다.

| 도구 | 역할 |
|---|---|
| `get_machine_health` | 장비 스냅샷(점수, 불량별 순위, 원인변수, 미확인 이상) |
| `get_defect_causes` | 특정 불량의 원인변수와 메커니즘 |
| `get_sop_for_factor` | 조치 방법 |
| `get_defect_occurrence_dates` | 실제 불량 발생 이력 |
| `get_trend_chart_data` | 그래프용 시계열 + 기준선/경보선 |

### 출력 예시

```
🟡 DP02 · 1순위 - Chipping (HI 47.6)
- 발생: 없음(조짐 단계)
- 원인: Laser_Power 현재 18.44 / 관리한계 18.25,
        하강추세, 2026-02-02부터 56.0일째 경보 지속
- 메커니즘: Laser_Power 감소 → Chipping 증가
- 조치: 즉시조치(tier T1) — 위험구간 17.79 ~ 18.37
```

**"발생"과 "조짐"을 구분합니다.** DP02·DP03은 불량이 아직 안 났는데 56일·42일째 경보 중입니다 — 조기경보가 의도대로 동작하는 상태입니다. **멘토 정답이 DP02 = Laser Aging → Chipping이므로, 라벨이 안 났을 뿐 조짐 판정이 맞았습니다.**

> ⚠️ 현재 agent는 `lsl`/`usl`(멘토 스펙)을 읽어 표시하는데 점수는 관리한계 기준이라
> 두 숫자의 기준이 어긋납니다. `control_lsl`을 읽도록 고쳐야 합니다(미반영).

### 근거 표시

모든 수치에 출처를 붙입니다.

| 꼬리표 | 의미 |
|---|---|
| `[스펙기준]` | 멘토 제공 스펙 (10개 변수) — 표시용 절대 기준 |
| `[관리한계]` | 정상값 ± 3σ — **점수를 내는 선** (전 변수) |
| 위험구간 진입률 | 불량 데이터로 학습한 경계 (C유형 5개) — 참고 지표 |

엔지니어가 "이 숫자를 얼마나 믿어야 하나"를 판단할 수 있게 하기 위해서입니다.

---

## 8. 성과

| | |
|---|---|
| 확인된 열화 | **11건** (전부 Mann-Kendall 독립 검정과 일치) |
| 최장 지속 탐지 | DP02 Power_Efficiency **59.5일** |
| 스펙 검사로 잡히는 수 | **0건** |
| 이동 폭 중앙값 | **0.57σ** (샷 표준편차 기준) |
| 화면에 뜨는 경보 | **44건** (원시 경보 51,039행 → 사건 5,929 → 활성 상태 44) |
| 그중 강한 경보(`full`) | **9건** — 나머지 35건은 `early`라 흐리게 표시 |
| **멘토 정답 대조** | **장비 4/4 일치**, 원인 변수 3/3 일치 |
| **탐지 선행** | 실제 열화보다 **4~18일** 빠름 |
| **오탐** | 정상 장비(DP01) 14일+ 지속 경보 **0건** |

---

## 9. 한계 — 정직하게 말할 것

1. **Micro_Crack을 감시하지 못합니다.** 멘토 시나리오상 DP03의 불량인데, 원인 인자 2개
   (Cooling_Flow / Cooling_Water_Temp)가 원본에서 도메인 기대와 **반대 방향**입니다.
   냉각 변수 자체가 89일 동안 ±0.09σ밖에 안 움직여 이 데이터로는 확인이 불가능합니다.
   **가짜 100점을 내지 않도록 명시적으로 제외하고 경고를 출력합니다.**

2. **Chipping 라벨이 4건, Laser_Paim이 0건입니다.** 시나리오는 주입됐는데 감시 데이터에
   불량 라벨이 거의 안 실려 있습니다. 원인 드리프트는 또렷하게 보이지만(DP02
   `PE −0.56σ → Kerf +0.62σ`) 결과 라벨로는 검증이 안 됩니다. Chipping 경계는 별도
   학습셋(r1)에서 배웠고, `Laser_Power ↔ Chipping`만 감시 데이터 이항검정을 통과했습니다
   (4/4가 위험구간, p=9.5e-05).

3. **다변량 FDC를 안 씁니다.** 실무 FDC의 주류는 PCA/PLS + Hotelling T² / SPE인데
   저희는 변수별 단변량 감시입니다.

4. **일부 상수는 정책값입니다.** `ALARM_BAND = 10`(점수 하단 구간 폭)은 표시 스케일을
   어떻게 쪼갤지의 문제라 물리 근거가 없습니다. 다만 **순위는 상수에 안 걸립니다** —
   전부 흔들어도 `DP04 < DP02 < DP03 < DP01`이 유지되고, **절대값만 3배 넘게 흔들립니다.**
   그래서 발표에서 점수를 단정하지 않고 순위와 근거로 말합니다.

5. **Feed_Speed는 4대 모두 Cpk 0.83**입니다(1.0 미만 = 공정능력 부족). 장비 문제가 아니라
   스펙 대비 공정 산포가 큰 것이라 알림으로 해결되지 않습니다 — 스펙 또는 공정 조건
   재검토가 필요합니다.

---

## 10. 예상 질문

**Q. 왜 CUSUM인가? 관리도(3σ)로는 안 되나**
3σ 관리도는 한 점이 선을 넘는지만 봅니다. 우리 열화는 0.57σ라 절대 안 걸립니다. CUSUM은 작은 편차를 누적하므로 방향성 있는 이동을 잡습니다.
**그래서 역할을 나눴습니다 — 탐지는 CUSUM, 심각도 척도는 3σ 관리한계.** 점수의 100%
지점을 CUSUM 경보선(0.7σ)에 걸었더니 경보가 확정된 컬럼이 무엇이든 바닥을 쳤습니다
(DP02 Laser_Power가 목표값에서 0.72σ인데 9.7점). 탐지 문턱은 심각도 자가 될 수 없습니다.

**Q. 왜 CUSUM K=0.7 / H=4.5인가? 교과서는 0.5 / 4 아닌가**
맞습니다. 교과서값으로 되돌려 실측했더니 **정상 장비 DP01이 89일 중 87.6일 경보 상태**가
되어 고장 장비와 구분이 안 됐습니다(14일+ 지속 경보 DP01 8개 vs DP02 14 / DP03 14 /
DP04 12). 0.7 / 4.5에서는 DP01이 **0개**입니다.

**Q. 오경보가 많지 않나**
추세 없는 장비에서의 오경보율을 전 변수에 대해 측정했습니다 — 0.000~0.364%로 전부 기준(1%) 미만입니다. 그리고 매 샷 알림이 아니라 상태로 관리하므로 화면에 뜨는 건 75건입니다.

**Q. 스펙이 없는 변수는 어떻게 판정하나**
**전 변수 동일하게 관리한계(정상값 ± 3σ)로 판정합니다.** 스펙 유무와 무관합니다.
스펙은 "이걸 넘으면 불량품"이라는 요구사항이라 데이터로 만들 수 없고, 있어도 점수
기준으로는 못 씁니다(자연 변동폭의 50~100배라 아무리 나빠져도 8.6%). 멘토 스펙은
절대 기준으로 화면에 같이 보여주기만 합니다.

**Q. FDC 데이터는 원래 스펙이 없지 않나**
아닙니다 — **멘토 스펙 10개 중 7개가 FDC**입니다(Laser_Power, Power_Efficiency,
Laser_Centering_Position, Frequency, Feed_Speed, Head_Temp, Focus). 다만 **성격이
다릅니다**: response 스펙은 "이 값을 벗어나면 불량품"(설계/고객)이고, FDC 스펙은
"레시피 설정값 ± 허용범위"(공정/장비)입니다. Laser_Power 17.8~19.2는 운전 조건이지
"18.4면 불량품"이 아닙니다. **그래서 FDC 스펙 안에서 일어나는 열화를 못 잡습니다.**

**Q. 이 경보를 받으면 뭘 해야 하나**
관계DB에 tier(T1 즉시조치 / T2 조건부 / T3 감시)와 조치 방향이 있습니다. 다만 **구체적 SOP는 아직 멘토 미수령**이라 현재는 `[SOP 미수령]`으로 표시합니다.

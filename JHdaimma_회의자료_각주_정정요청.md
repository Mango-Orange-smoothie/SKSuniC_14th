# JHdaimma님께 — 회의자료에 낡은 대조표가 2곳 남아 있습니다

**대상**: `26.08.05_Goal2_통합_Relationship_DB_JHdaimma/회의자료_라벨확장안_결정요청.md`
**제가 안 고친 이유**: 진혁님 문서라 알려드리는 게 맞다고 봤습니다.

머리말 각주는 `5d966e5`의 "✅ 적용 완료" 배너로 이미 정리해주셨습니다. 남은 건 본문 2곳인데,
그 사이 라벨 확장안까지 들어가면서 **두 번 낡았습니다.**

---

## 지금 값 (직접 세었습니다 — `agent_cause_factors.json`)

```
T1 / 즉시조치      3건   Power_Efficiency · Laser_Power · Head_Temp
T2 / 조건부조치    3건   CLN_Pressure · CLN_Flow · Cooling_Flow
급락알람           0건

CLN_Pressure  최상위 T2 / 조건부조치
              per_defect  Remain_Coat T1·즉시조치  /  Particle T2·조건부조치
```

## 고칠 곳 2군데

**179~184행 — "📌 main 기준으로 보면 숫자가 다릅니다" 절**

```
현재 문구:  origin/main에는 CLN_Pressure가 아직 급락알람으로 들어 있습니다.
            | origin/main (지금) | spike | 급락알람 | 3개 |
            | JHdaimma 브랜치    | level | 즉시조치 | 4개 |
```

두 줄 다 이제 안 맞습니다.

- `origin/main` 줄 — PR #23이 병합돼서 main은 `level` / `즉시조치`입니다. `급락알람`은 DB 전체에 0건입니다.
- `JHdaimma 브랜치` 줄 — PR #24로 `CLN_Pressure`에 Particle이 붙으면서 보수적 병합으로 **T2 / 조건부조치**가 됐습니다. 즉시조치는 4개가 아니라 **3개**입니다. 진혁님이 커밋 메시지에 "즉시조치 인자 4개 → 3개"라고 적으신 그 변화입니다.

이 절은 "main과 브랜치가 다르다"를 설명하려고 만든 건데 둘이 이미 같아졌으니, **절 전체를 지우는 게 맞아 보입니다.** 남겨두면 지금 3개인 걸 4개로 읽습니다.

**243~244행 — 결정표 각주**

```
현재 문구:  ※ JHdaimma 브랜치 기준입니다. origin/main은 CLN_Pressure가
            아직 급락알람이라 3개입니다. 554816b 병합이 먼저 되면 4개가 됩니다.
```

같은 이유로 삭제 대상입니다. 결론적으로 숫자(3개)는 맞는데 사유가 완전히 반대라, 읽는 사람이
"병합되면 4개가 되겠구나"로 잘못 이해합니다.

**본문 숫자와 판단은 손댈 게 없습니다** — 낡은 건 이 대조표뿐입니다.

---

## 참고 — 저희 쪽 반영 결과

PR #24를 받아 파이프라인을 다시 돌렸습니다. 예측하신 값 그대로 나왔습니다.

```
DP04 Particle    95.0 -> 14.5
Particle 원인    Surface_Roughness 하나 -> CLN_Flow · CLN_Pressure · Surface_Roughness
Micro_Crack      원인 0개 -> Cooling_Flow (장비 대표 점수에는 여전히 불참)
장비 점수         DP01 91.9 -> 85.0, DP02 47.6 / DP03 50.8 / DP04 14.5 불변
```

DP01이 내려간 건 나빠진 게 아니라 **안 보이던 게 보인 것**입니다. `CLN_Flow`가 Particle
원인으로도 잡히면서 그 짝의 경계로 경보가 새로 떴습니다(DP01 0일 → 5.2일, DP03 0일 → 10.0일).

**한 인자에 defect가 둘인 경우를 이번에 짝 단위로 처리하도록 고쳤습니다.** 안 고쳤으면
`dict` 대입이라 파일 뒷줄이 앞줄을 덮어써서 `CLN_Flow↔Remain_Coat`와
`CLN_Pressure↔Remain_Coat`(둘 다 T1 확정)가 통째로 사라지고 Remain_Coat가 "원인 없는
defect"가 될 뻔했습니다. 재현 스크립트는 `docs/check_multi_defect_pairing.py`입니다.

## 알려드릴 것 — `CLN_Flow↔Particle`의 반대 방향 그룹을 제외했습니다

이 짝은 그룹마다 위험 방향이 갈렸습니다. 감시 데이터로 재보니 **54개 그룹 중 47개는
"낮으면 위험", 7개는 "높으면 위험"**이었습니다.

**세정 유량이 높아서 파티클이 는다는 건 물리적으로 말이 안 되므로, 그 7개에서 잡힌 건
이 인자가 아니라 다른 원인으로 보고 제외했습니다.** 도메인 판단입니다.

구현은 짝을 특정해 하드코딩하지 않았습니다 — `rel_30.threshold_direction`을 읽어서
그룹별 학습 방향과 대조하고, 반대인 그룹은 `00_baseline_C.csv`에 행을 안 만듭니다.
**방향의 단일 출처도 관계DB입니다.** 다른 짝은 전부 DB 방향과 일치해서 영향이 없습니다
(Surface_Roughness↔Particle 54/54 high, 나머지 54/54 low).

바뀐 것:

```
DP03 CLN_Flow      경보 10.0일 -> 없음,  변수 점수 71.1 -> 100.0   (오탐이었음)
CLN_Flow↔Particle  경보행 9,941 -> 9,701
early 경보          66건 -> 65건
장비 점수           85.0 / 47.6 / 50.8 / 14.5  변동 없음
```

DP01 CLN_Flow의 5.2일 경보는 정상 방향 그룹에서 나온 것이라 그대로 남습니다.

**rel_30 쪽에서 이 짝의 `risk_ratio`(3.92)나 경계값(9.722)을 다시 산출하실 일이 있으면,
반대 방향 7개 그룹이 포함된 값인지 확인해주시면 좋겠습니다.** 저희 쪽 경계값은 이제
47개 그룹 기준입니다.
